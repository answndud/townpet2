# 2026-01-23 invalid-posttype-filter

## 요약
- 증상: 피드 조회 시 `PostType` enum 오류 발생
- 영향 범위: `/` 피드 렌더링 실패
- 심각도: 중간 (페이지 렌더링 차단)

## 재현 조건
- 단계: `/` 페이지 진입 시 `type` 쿼리 파라미터 포함
- 환경: Prisma enum 필터에 잘못된 값 전달

## 원인 분석
- 직접 원인: URL 쿼리 값이 검증되지 않은 채 Prisma 필터에 전달
- 근본 원인: 서버 컴포넌트에서 Zod 검증 누락

## 해결 방법
- 수정 내용: `postListSchema`로 쿼리 파라미터 검증 후 필터 적용
- 관련 파일: `app/src/app/page.tsx`, `app/src/server/queries/post.queries.ts`
- 관련 PR/커밋: 없음

## 대응 구분
- 임시 대응(워크어라운드): 잘못된 쿼리 파라미터 제거
- 영구 대응(근본 수정): 서버 컴포넌트에서 Zod 검증 적용

## 재발 방지
- 테스트/모니터링 보강: 쿼리 파라미터 검증 테스트 추가
- 정책/문서 업데이트: 필터 쿼리는 Zod 검증 필수
