# 보안 진행상황

Last updated: 2026-03-06

## Working agreement
- This file stores security-track execution logs only.
- Each entry should include changed files and verification evidence.
- Keep `PLAN.md`/`PROGRESS.md` synchronized with a short pointer update.

## Execution log

### 2026-03-06: SEC-016 로그인 abuse observability + progressive backoff
- Completed
- 로그인 감사 로그 확장:
  - `app/prisma/schema.prisma`
  - `app/prisma/migrations/20260306133000_expand_auth_audit_for_login_events/migration.sql`
  - `AuthAuditLog`가 login success/failure/rate-limited 이벤트를 수용하도록 확장되고, 마스킹 식별자/사유 코드를 저장
- credentials auth path 보강:
  - `app/src/server/auth-credentials.ts`
  - `app/src/lib/auth.ts`
  - 로그인 성공/실패/rate-limit 이벤트를 감사 로그로 남기고, 계정+IP 시도 횟수 기반 progressive delay를 적용
- admin visibility:
  - `app/src/server/queries/auth-audit.queries.ts`
  - `app/src/app/admin/auth-audits/page.tsx`
  - `app/src/app/api/admin/auth-audits/export/route.ts`
  - 운영 화면과 CSV export에서 로그인 실패 사유/식별자를 조회 가능하도록 확장
- Regression tests
  - `app/src/server/auth-credentials.test.ts`
  - `app/src/server/auth-login-identifier.test.ts`
  - `app/src/app/api/admin/auth-audits/export/route.test.ts`
  - `app/src/app/api/admin/auth-audits/route.test.ts`
- Verification
  - `pnpm -C app exec prisma generate`
  - `pnpm -C app lint src/lib/auth.ts src/server/auth-credentials.ts src/server/auth-credentials.test.ts src/server/auth-audit-log.ts src/server/auth-login-identifier.ts src/server/auth-login-identifier.test.ts src/server/auth-login-rate-limit.ts src/server/rate-limit.ts src/server/queries/auth-audit.queries.ts src/app/admin/auth-audits/page.tsx src/app/api/admin/auth-audits/export/route.ts src/app/api/admin/auth-audits/export/route.test.ts src/app/api/admin/auth-audits/route.test.ts`
  - `pnpm -C app typecheck`
  - `pnpm -C app test -- src/server/auth-credentials.test.ts src/server/auth-login-identifier.test.ts src/app/api/admin/auth-audits/export/route.test.ts src/app/api/admin/auth-audits/route.test.ts src/server/auth.service.test.ts`
- Blockers
  - 운영 DB에 새 auth audit migration 적용 필요 (`pnpm prisma migrate deploy`)

### 2026-03-06: SEC-015 정지 계정 write-path enforcement parity
- Completed
- 공용 interaction sanction 가드 추가:
  - `app/src/server/services/sanction.service.ts`
  - `assertUserInteractionAllowed()` helper로 active sanction을 일관되게 403 surface
- 서비스 레이어 enforcement:
  - `app/src/server/services/post.service.ts`
  - `app/src/server/services/comment.service.ts`
  - 글/댓글 작성·수정·삭제 및 반응 경로에서 direct REST/API 진입도 제재 우회가 불가능하도록 보강
- 업로드 route parity:
  - `app/src/app/api/upload/route.ts`
  - `app/src/app/api/upload/client/route.ts`
  - 인증 사용자 업로드도 sanction 상태를 존중
- Regression tests
  - `app/src/server/services/sanction.service.test.ts`
  - `app/src/server/services/post-create-policy.test.ts`
  - `app/src/server/services/comment.service.test.ts`
  - `app/src/app/api/upload/route.test.ts`
  - `app/src/app/api/upload/client/route.test.ts`
  - `app/src/server/auth.test.ts`
- Verification
  - `pnpm -C app lint src/server/services/sanction.service.ts src/server/services/post.service.ts src/server/services/comment.service.ts src/app/api/upload/route.ts src/app/api/upload/client/route.ts src/server/services/sanction.service.test.ts src/server/services/post-create-policy.test.ts src/server/services/comment.service.test.ts src/app/api/upload/route.test.ts src/app/api/upload/client/route.test.ts src/server/auth.test.ts`
  - `pnpm -C app test -- src/server/services/sanction.service.test.ts src/server/services/post-create-policy.test.ts src/server/services/comment.service.test.ts src/app/api/upload/route.test.ts src/app/api/upload/client/route.test.ts src/server/auth.test.ts`
  - `pnpm -C app typecheck`
- Blockers
  - None in code. Remaining moderation realism work is tracked separately in `PLAN.md` Cycle 199~205.

### 2026-03-06: SEC-014 credential change session revocation
- Completed
- 스키마/마이그레이션:
  - `app/prisma/schema.prisma`
  - `app/prisma/migrations/20260306120000_add_user_session_version/migration.sql`
  - `User.sessionVersion` 추가
- auth JWT invalidation:
  - `app/src/lib/auth.ts`
  - `app/src/lib/session-version.ts`
  - 세션 토큰이 현재 version과 mismatch 시 인증 사용자 정보를 제거해 기존 세션을 무효화
- credential change version bump:
  - `app/src/server/services/auth.service.ts`
  - 기존 비밀번호 변경 및 비밀번호 reset confirm 시 `sessionVersion` 증가
- Regression tests
  - `app/src/lib/session-version.test.ts`
  - `app/src/server/services/auth.service.test.ts`
- Verification
  - `pnpm -C app exec prisma generate`
  - `pnpm -C app lint src/lib/auth.ts src/lib/session-version.ts src/lib/session-version.test.ts src/server/services/auth.service.ts src/server/services/auth.service.test.ts`
  - `pnpm -C app test -- src/lib/session-version.test.ts src/server/services/auth.service.test.ts`
  - `pnpm -C app typecheck`
- Blockers
  - 운영 DB에 실제 적용하려면 `pnpm prisma migrate deploy`가 필요.

### 2026-03-06: SEC-013 배포 보안 체크리스트 확장 정리
- Completed
- 배포 보안 체크리스트를 `manual-checks` 경로의 실사용 문서로 재구성
- 포함 내용:
  - Vercel production final required secrets
  - 사용자가 공유한 현재 Vercel / GitHub Actions 설정 현황
  - strict preflight `fail=5` 기준 환경 슬롯 확인 순서
- 기존 `docs/operations/manual-checks/배포_보안_체크리스트.md`는 안내 링크 문서로 단순화
- Changed files
- `docs/operations/manual-checks/배포_보안_체크리스트.md`
- `docs/operations/manual-checks/배포_보안_체크리스트.md`
- `docs/개발_운영_가이드.md`
- Verification
- Documentation-only update
- Blockers
- None

### 2026-03-06: SEC-013 운영 env 템플릿 추가
- Completed
- production environment template file added:
  - `app/.env.production.example`
- security checklist/GUIDE에서 템플릿 경로를 바로 찾을 수 있도록 연결
- Changed files
- `app/.env.production.example`
- `docs/operations/manual-checks/배포_보안_체크리스트.md`
- `docs/개발_운영_가이드.md`
- Verification
- Documentation/template-only update
- Blockers
- None

### 2026-03-06: SEC-013 후속 운영 프리플라이트 고정
- Completed
- production strict 보안 점검을 package script로 고정:
  - `pnpm -C app ops:check:security-env:strict`
- 운영자용 체크리스트 문서 추가:
  - `docs/operations/manual-checks/배포_보안_체크리스트.md`
- 현재 로컬 env 기준 strict preflight를 실행해 fail 항목 5개를 확인했고, 문서에 그대로 기록:
  - weak/missing auth secret
  - `CSP_ENFORCE_STRICT`
  - `GUEST_HASH_PEPPER`
  - `HEALTH_INTERNAL_TOKEN`
  - `UPSTASH_REDIS_REST_URL_AND_TOKEN_PAIR`
- Changed files
- `app/package.json`
- `docs/operations/manual-checks/배포_보안_체크리스트.md`
- `docs/개발_운영_가이드.md`
- Verification
- `pnpm -C app run | rg 'ops:check:security-env'`
- `pnpm -C app ops:check:security-env:strict`
- Blockers
- None in code. 실제 production env 반영은 외부 운영 단계.

### 2026-03-06: SEC-010/011/012/013 배포 전 보안 하드닝 후속
- Completed
- 사용자 생성 이미지의 출처를 1st-party 업로드로 제한:
  - `upload-url` 헬퍼를 추가해 `/uploads/*` 및 Vercel Blob public upload URL만 신뢰
  - 게시글 validation에서 외부 이미지 URL을 거부
  - 마크다운 렌더링에서 외부 이미지 토큰은 `<img>`로 렌더링하지 않음
  - Blob client upload token 발급 전에 `uploads/` 경로만 허용
- 관리자 auth audit CSV export에 formula injection 방어 추가:
  - 수식 시작 패턴 셀 앞에 `'`를 붙여 spreadsheet 실행을 차단
- CSP `script-src` 실효성 강화:
  - production strict/report-only 모두에서 광역 `https:` script 허용 제거
- 운영 보안 env와 dev social login 정책 정렬:
  - production validation이 `CSP_ENFORCE_STRICT`, `GUEST_HASH_PEPPER`, `HEALTH_INTERNAL_TOKEN`, `UPSTASH_REDIS_*`, weak auth secret을 강제
  - dev social login은 `ENABLE_SOCIAL_DEV_LOGIN=1` explicit opt-in으로 전환
- Changed files
- `app/src/lib/upload-url.ts`
- `app/src/lib/upload-url.test.ts`
- `app/src/lib/validations/post.ts`
- `app/src/lib/validations/post.test.ts`
- `app/src/lib/markdown-lite.ts`
- `app/src/lib/markdown-lite.test.ts`
- `app/src/app/api/upload/client/route.ts`
- `app/src/app/api/upload/client/route.test.ts`
- `app/src/app/api/admin/auth-audits/export/route.ts`
- `app/src/app/api/admin/auth-audits/export/route.test.ts`
- `app/middleware.ts`
- `app/src/middleware.test.ts`
- `app/src/lib/env.ts`
- `app/src/lib/env.test.ts`
- `app/src/lib/auth.ts`
- `app/src/app/login/page.tsx`
- `app/src/app/register/page.tsx`
- Verification
- `pnpm -C app lint src/lib/upload-url.ts src/lib/upload-url.test.ts src/lib/env.ts src/lib/env.test.ts src/lib/validations/post.ts src/lib/validations/post.test.ts src/lib/markdown-lite.ts src/lib/markdown-lite.test.ts src/app/api/upload/client/route.ts src/app/api/upload/client/route.test.ts src/app/api/admin/auth-audits/export/route.ts src/app/api/admin/auth-audits/export/route.test.ts src/lib/auth.ts src/app/login/page.tsx src/app/register/page.tsx middleware.ts src/middleware.test.ts`
- `pnpm -C app test -- src/lib/upload-url.test.ts src/lib/env.test.ts src/lib/validations/post.test.ts src/lib/markdown-lite.test.ts src/app/api/upload/client/route.test.ts src/app/api/admin/auth-audits/export/route.test.ts src/middleware.test.ts`
- `pnpm -C app typecheck`
- `pnpm -C app ops:check:security-env`
- `env NODE_ENV=production DATABASE_URL=postgres://db AUTH_SECRET=abcdefghijklmnopqrstuvwxyz1234567890ABCD APP_BASE_URL=https://townpet.dev CSP_ENFORCE_STRICT=1 GUEST_HASH_PEPPER=pepper-secret HEALTH_INTERNAL_TOKEN=health-secret UPSTASH_REDIS_REST_URL=https://upstash.example.com UPSTASH_REDIS_REST_TOKEN=token pnpm -C app ops:check:security-env`
- Blockers
- None. 남은 작업은 실제 배포 환경에 strict 보안 env를 반영하는 운영 단계.

### 2026-03-05: SEC-008/009 의존성 취약점 패치 + 보안 env preflight 자동화
- Completed
- 의존성 보안 패치:
  - `next` `16.1.4 -> 16.1.5`
  - `eslint-config-next` `16.1.4 -> 16.1.5`
  - `@vercel/blob` `1.1.1 -> 2.3.1`
- `pnpm -C app audit --prod` 재검증에서 known vulnerability `0건` 확인.
- 운영 보안 설정 drift 방지를 위해 `ops:check:security-env` 스크립트를 추가:
  - 점검 대상: `AUTH_SECRET/NEXTAUTH_SECRET`, `CSP_ENFORCE_STRICT`, `GUEST_HASH_PEPPER`, `HEALTH_INTERNAL_TOKEN`, `UPSTASH_REDIS_REST_URL/TOKEN`
  - `SECURITY_ENV_STRICT=1` 또는 `NODE_ENV=production`에서 실패 기준을 엄격 적용.
- 운영 문서(`docs/개발_운영_가이드.md`)에 실행 명령(기본/엄격 모드) 반영.
- Changed files
- `app/package.json`
- `app/pnpm-lock.yaml`
- `app/scripts/check-security-env.ts`
- `docs/개발_운영_가이드.md`
- Verification
- `pnpm -C app audit --prod`
- `pnpm -C app lint scripts/check-security-env.ts src/lib/env.ts src/lib/auth.ts middleware.ts src/app/api/upload/client/route.ts`
- `pnpm -C app typecheck`
- `pnpm -C app test -- src/app/api/upload/client/route.test.ts src/middleware.test.ts src/app/api/health/route.test.ts`
- `pnpm -C app ops:check:security-env`
- `NODE_ENV=production SECURITY_ENV_STRICT=1 AUTH_SECRET='<strong-secret>' CSP_ENFORCE_STRICT=1 GUEST_HASH_PEPPER='<pepper>' HEALTH_INTERNAL_TOKEN='<token>' UPSTASH_REDIS_REST_URL='<url>' UPSTASH_REDIS_REST_TOKEN='<token>' pnpm -C app ops:check:security-env`
- Blockers
- None (코드 기준). 운영 환경에서 strict 모드 점검은 실제 시크릿 설정 반영 후 수행 필요.

### 2026-03-04: SEC-002 CSP 하드닝 2차 완료(strict enforce nonce 경로 검증)
- Completed (Phase 2)
- nonce 전파를 `x-csp-nonce` 단일 헤더에서 `x-nonce` + `x-csp-nonce` 동시 설정으로 보강해 Next nonce 적용 경로와 정합을 맞춤.
- JSON-LD 인라인 스크립트 경로를 nonce 기반으로 보강:
  - `/posts/[id]`(회원 상세): server page에서 nonce를 client component로 전달
  - `/users/[id]`(공개 프로필): server component에서 nonce 주입
  - `/posts/[id]/guest`(게스트 상세): dynamic 전환 후 nonce 주입
- strict enforce 실측:
  - `CSP_ENFORCE_STRICT=1 pnpm -C app start --port 3105` 실행
  - `curl`로 응답 헤더 `script-src 'self' 'nonce-...' https:` 확인
  - `application/ld+json` 스크립트 nonce 포함 확인(게스트 상세/공개 프로필)
- 회귀 검증:
  - `pnpm -C app quality:gate` 통과
  - `pnpm -C app build` 통과
- Changed files
- `app/middleware.ts`
- `app/src/lib/csp-nonce.ts`
- `app/src/app/posts/[id]/page.tsx`
- `app/src/components/posts/post-detail-client.tsx`
- `app/src/app/users/[id]/page.tsx`
- `app/src/app/posts/[id]/guest/page.tsx`
- Blockers
- None (코드/로컬 검증 기준). 운영 환경 위반 추이는 기존 `/api/security/csp-report` 모니터링 경로로 추적 지속.

### 2026-02-26: SEC-006 비회원 식별 해시 HMAC(pepper) 전환 + legacy 호환
- Completed
- guest identity 해시 경로를 강화:
  - 신규 기본: `GUEST_HASH_PEPPER` 기반 HMAC-SHA256
  - 호환성: legacy SHA-256도 후보로 함께 조회/매칭
- ban/violation 조회와 guest ownership 비교에서 hash candidate 목록을 사용해 기존 데이터와의 호환성을 유지.
- 저장은 peppered primary hash를 우선 사용하고, 조회는 peppered+legacy 후보를 모두 고려.
- guest safety 전용 테스트를 추가하고 기존 guest management 회귀 테스트를 재검증.
- Changed files
- `app/src/server/services/guest-safety.service.ts`
- `app/src/server/services/guest-safety.service.test.ts`
- `app/src/server/services/post.service.ts`
- `app/src/server/services/comment.service.ts`
- Verification
- `cd app && pnpm lint "src/server/services/guest-safety.service.ts" "src/server/services/guest-safety.service.test.ts" "src/server/services/post.service.ts" "src/server/services/comment.service.ts"`
- `cd app && pnpm test -- "src/server/services/guest-safety.service.test.ts" "src/server/services/guest-post-management.service.test.ts" "src/server/services/guest-comment-management.service.test.ts"`
- `cd app && pnpm typecheck`
- Blockers
- 운영 환경에 `GUEST_HASH_PEPPER`가 설정되지 않으면 legacy SHA-256 경로로 동작하므로, 보안효과 극대화를 위해 시크릿 설정이 필요.

### 2026-02-26: SEC-007 회원가입 중복 응답 enumeration 완화
- Completed
- `POST /api/auth/register`에서 서비스 레이어의 `EMAIL_TAKEN`/`NICKNAME_TAKEN`을 외부 응답으로 그대로 노출하지 않도록 정규화.
- 공개 계약 변경:
  - duplicate email / duplicate nickname 모두 `400 REGISTER_REJECTED`로 통일
  - 메시지: `회원가입 정보를 확인해 주세요.`
- 계약 테스트를 보강해 이메일/닉네임 중복 모두 동일 응답으로 고정.
- Changed files
- `app/src/app/api/auth/register/route.ts`
- `app/src/app/api/auth/register/route.test.ts`
- Verification
- `cd app && pnpm lint "src/app/api/auth/register/route.ts" "src/app/api/auth/register/route.test.ts"`
- `cd app && pnpm test -- "src/app/api/auth/register/route.test.ts"`
- `cd app && pnpm typecheck`
- Blockers
- None.

### 2026-02-26: SEC-003 비밀번호 정책 강화 + 유출 비밀번호 차단
- Completed
- 비밀번호 정책을 `register/password setup/password reset confirm` 경로에서 강화:
  - 최소 10자, 최대 72자
  - 소문자/대문자/숫자/특수문자 포함
  - 반복 문자 과다 패턴 차단
  - 로컬 breached deny-list 차단
- 로그인 스키마는 기존 계정 호환성을 위해 강제 복잡도 체크를 적용하지 않도록 분리.
- validation/unit 테스트와 회원가입 route 계약 테스트를 새 정책에 맞춰 갱신.
- Changed files
- `app/src/lib/validations/auth.ts`
- `app/src/lib/validations/auth.test.ts`
- `app/src/app/api/auth/register/route.test.ts`
- Verification
- `cd app && pnpm lint "src/lib/validations/auth.ts" "src/lib/validations/auth.test.ts" "src/app/api/auth/register/route.test.ts"`
- `cd app && pnpm test -- "src/lib/validations/auth.test.ts" "src/app/api/auth/register/route.test.ts"`
- `cd app && pnpm typecheck`
- Blockers
- 글로벌 breach API(HIBP) 연동은 후속 결정 필요(현재는 로컬 deny-list 기반).

### 2026-02-26: SEC-004 로그인 락아웃 에스컬레이션(account + IP)
- Completed
- Credentials 로그인 경로에 다중 윈도우 rate-limit 규칙을 추가:
  - `auth:login:ip:<ip>` (10/min)
  - `auth:login:account-ip:<emailHash>:<ip>` (5/15m)
  - `auth:login:account:<emailHash>` (30/24h)
- 계정 식별자는 원문 이메일 대신 정규화(lowercase/trim) + SHA-256 해시 키를 사용.
- 정책 함수를 `auth-login-rate-limit` 모듈로 분리해 테스트 가능하도록 구성.
- Changed files
- `app/src/lib/auth.ts`
- `app/src/server/auth-login-rate-limit.ts`
- `app/src/server/auth-login-rate-limit.test.ts`
- Verification
- `cd app && pnpm lint "src/lib/auth.ts" "src/server/auth-login-rate-limit.ts" "src/server/auth-login-rate-limit.test.ts"`
- `cd app && pnpm test -- "src/server/auth-login-rate-limit.test.ts"`
- `cd app && pnpm typecheck`
- Blockers
- None.

### 2026-02-25: SEC-002 CSP 하드닝 1차(report-only strict + enforce flag)
- Completed (Phase 1)
- Production CSP rollout strategy를 2단계로 분리:
  - 기본: 기존 완화 CSP를 `content-security-policy`에 유지
  - 병행: strict CSP를 `content-security-policy-report-only`로 송출
- 강제 전환 플래그 추가:
  - `CSP_ENFORCE_STRICT=1` 설정 시 strict CSP를 즉시 enforce
- 정책 결정 함수(`resolveCspHeaders`)를 분리해 테스트 가능 구조로 정리.
- Added middleware-level tests for production/default strict report-only, strict enforce, and development behavior.
- Changed files
- `app/middleware.ts`
- `app/src/middleware.test.ts`
- Verification
- `cd app && pnpm lint "middleware.ts" "src/middleware.test.ts"`
- `cd app && pnpm test -- "src/middleware.test.ts"`
- `cd app && pnpm typecheck`
- Blockers
- strict CSP 완전 전환(Phase 2)을 위해 nonce/hash 적용 대상 스크립트 인벤토리와 위반 리포트 분석이 필요.

### 2026-02-25: SEC-005 신뢰 프록시 기준 client IP 파싱 강화
- Completed
- `getClientIp`를 trusted proxy hops 기반으로 재구성.
- 동작 정책:
  - production 기본값: `TRUSTED_PROXY_HOPS=1`로 간주
  - development/test 기본값: `TRUSTED_PROXY_HOPS=0`
  - 명시 override: `TRUSTED_PROXY_HOPS` (0~5)
- `x-forwarded-for` 체인에서 trusted proxy hop을 고려해 client IP를 선택하고, 없으면 `x-real-ip`/`anonymous` fallback 유지.
- Regression tests added for dev/prod/override cases.
- Changed files
- `app/src/server/request-context.ts`
- `app/src/server/request-context.test.ts`
- Verification
- `cd app && pnpm lint "src/server/request-context.ts" "src/server/request-context.test.ts"`
- `cd app && pnpm test -- "src/server/request-context.test.ts"`
- `cd app && pnpm typecheck`
- Blockers
- None.

### 2026-02-25: SEC-001 `/api/health` 민감 정보 노출 최소화
- Completed
- Public health response에서 상세 진단 정보를 기본 비노출로 전환:
  - `env.missing` 제거(공개 요청)
  - DB 오류 상세 메시지 제거(공개 요청)
  - rate limit 상세 `detail` 제거(공개 요청)
- 내부 진단이 필요한 경우에만 상세 필드 노출하도록 토큰 기반 게이트 추가:
  - `HEALTH_INTERNAL_TOKEN` 환경변수
  - `x-health-token` 또는 `Authorization: Bearer <token>` 일치 시 상세 노출
- Added regression tests for public vs internal response contracts.
- Changed files
- `app/src/app/api/health/route.ts`
- `app/src/app/api/health/route.test.ts`
- `app/src/lib/env.ts`
- Verification
- `cd app && pnpm lint "src/app/api/health/route.ts" "src/app/api/health/route.test.ts" "src/lib/env.ts"`
- `cd app && pnpm test -- "src/app/api/health/route.test.ts"`
- Blockers
- None.

### 2026-02-25: Security follow-up track bootstrap
- Completed
- Created dedicated security tracking files:
  - `docs/security/보안_계획.md`
  - `docs/security/보안_진행상황.md`
  - `docs/security/보안_위험_등록부.md`
  - `docs/security/보안_결정기록.md`
- Seeded top-7 hardening backlog from recent security review into the security plan.
- Added process rule updates in `AGENTS.md` to keep security work branched but synchronized.
- Verification
- Documentation-only update. No code/test execution required.
- Blockers
- None.
