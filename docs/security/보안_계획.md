# 보안 계획

Last updated: 2026-03-07
Owner: Codex (security track)

## Purpose
- Track security hardening work separately from the main delivery plan.
- Keep `PLAN.md` as the global source of truth, and use this file for security-only details.

## Status legend
- `pending`: not started
- `in_progress`: actively being implemented
- `blocked`: waiting on dependency/decision
- `done`: implemented and verified

## Security hardening backlog

| ID | Priority | Task | Risk addressed | Status | DoD | ETA |
|---|---|---|---|---|---|---|
| SEC-001 | P0 | Health endpoint response minimization (`/api/health`) | Internal env/db detail exposure | done | Public response no longer includes `env.missing` or raw db error detail; internal diagnostics requires valid internal token | 0.5-1d |
| SEC-002 | P0 | CSP hardening (`unsafe-inline` removal path) | XSS impact reduction | done | strict enforce(`CSP_ENFORCE_STRICT=1`)에서 nonce 기반 script 허용 경로(미들웨어/JSON-LD) 검증 + `quality:gate`/`build` 회귀 통과 | 1.5-3d |
| SEC-003 | P1 | Password policy uplift + breached password deny | Weak credential risk | done | Register/reset/set use strong password policy (length+complexity+deny-list), login flow keeps backward compatibility | 1-2d |
| SEC-004 | P1 | Login lockout escalation (account + IP dimensions) | Credential stuffing/bruteforce | done | Multi-window throttling keys (`ip`, `account+ip`, `account`) are applied on credentials login with tests | 1-1.5d |
| SEC-005 | P1 | Trusted proxy IP resolution policy | IP spoofing via header trust mismatch | done | `getClientIp` follows explicit trusted chain policy with prod fallback (`TRUSTED_PROXY_HOPS=1`) and test coverage | 0.5-1d |
| SEC-006 | P1 | Guest identity hash hardening (HMAC pepper) | Re-identification/privacy risk | done | Guest hashes support HMAC pepper with legacy SHA-256 fallback matching for existing rows | 1-2d |
| SEC-007 | P1 | Enumeration-safe auth responses + regression tests | Account existence leakage | done | Register API now normalizes duplicate email/nickname signals to same public error code and tests enforce behavior | 1-1.5d |
| SEC-008 | P0 | Dependency security patch uplift (`next`, `@vercel/blob`) | Known upstream vulnerabilities (DoS/resource exhaustion) | done | `pnpm audit --prod`에서 known vulnerability 0건, lint/typecheck/tests 회귀 통과 | 0.5-1d |
| SEC-009 | P1 | Security env preflight automation (`ops:check:security-env`) | Production misconfiguration drift | done | 운영 핵심 보안 변수(CSP/pepper/internal token/upstash/auth secret) 자동 점검 스크립트 추가 및 GUIDE 실행 절차 반영 | 0.5-1d |
| SEC-010 | P0 | User-generated image provenance enforcement | Passive IP/User-Agent leakage via external images | done | 게시글 이미지/업로드 토큰 경로가 1st-party 업로드 URL만 허용하고 외부 이미지 렌더링이 차단됨 | 0.5-1d |
| SEC-011 | P1 | Admin CSV formula neutralization | Spreadsheet formula injection on export | done | auth audit CSV export가 수식 시작 문자를 neutralize하고 회귀 테스트가 존재함 | 0.5d |
| SEC-012 | P1 | Social dev login explicit opt-in | Preview/staging auth bypass | done | dev social login은 `ENABLE_SOCIAL_DEV_LOGIN=1`일 때만 활성화되고 production에서는 validation이 거부함 | 0.5d |
| SEC-013 | P1 | Runtime env enforcement parity | Security preflight/runtime mismatch | done | production startup validation이 strict CSP/pepper/health token/upstash/weak secret을 함께 강제함 | 0.5-1d |
| SEC-014 | P0 | Credential change session revocation | Stolen session remains valid after password reset/change | done | `User.sessionVersion` 기반 JWT invalidation이 적용되고 비밀번호 변경/reset 시 version이 증가하며 테스트가 존재함 | 1-2d |
| SEC-015 | P0 | Write-path sanction enforcement parity | Suspended user bypass via direct REST/API write paths | done | 글/댓글/반응/업로드의 인증 write path가 active sanction을 서비스/route 레이어에서 일관되게 거부하고 테스트가 존재함 | 1-2d |
| SEC-016 | P1 | Login abuse observability + step-up defenses | Bruteforce detection/response is too shallow | done | 실패 로그인 이벤트가 운영 감사 경로 또는 동급 로그로 남고 backoff/CAPTCHA/lockout 중 최소 1개가 credentials 로그인에 적용됨 | 1.5-3d |
| SEC-020 | P1 | Registration abuse observability + multi-axis throttling | Signup spam/disposable account bursts | done | 회원가입 경로가 IP/fingerprint/email+IP/email throttling을 적용하고 등록 성공/거절/제한을 auth audit에서 추적 가능함 | 1-2d |
| SEC-021 | P1 | Production deploy security-env preflight gate | Misconfigured production env reaches deploy/build path | done | `build:vercel`가 production Vercel target에서 strict security-env preflight를 먼저 실행하고 failure-path 테스트가 존재함 | 0.5-1d |
| SEC-022 | P2 | Auth audit retention automation | Sensitive auth audit metadata retained indefinitely | done | 인증 감사 로그 cleanup helper/workflow가 추가되고 180일 retention 기준이 운영 문서와 동기화됨 | 0.5-1d |

## Execution order
1. SEC-001
2. SEC-005
3. SEC-002
4. SEC-004
5. SEC-003
6. SEC-006
7. SEC-007
8. SEC-008
9. SEC-009
10. SEC-014
11. SEC-015
12. SEC-016
13. SEC-020
14. SEC-021
15. SEC-022

## Dependency notes
- SEC-002 should begin with report-only telemetry before enforce mode.
- SEC-006 requires a new secret (pepper) and fallback strategy for existing hashed data.
- SEC-007 should be coordinated with product UX copy to avoid confusion.
- SEC-009 strict mode는 `NODE_ENV=production SECURITY_ENV_STRICT=1` 조합으로 운영 기준 실패를 강제한다.
